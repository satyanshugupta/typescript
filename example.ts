function addShipping(price: number, shipping: number): number | null {
    return price + shipping;
}

// const addShipping = (price: number, shipping: number): number => {
//         return price + shipping;
//     }
console.log(addShipping(10, 5));